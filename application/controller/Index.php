<?php namespace app\controller;

use app\common\controller\Base;

class Index extends Base
{
    public function index()
    {
		// $GLOBALS['mod'] = $this->request->param('mod');
		// $GLOBALS['act'] = $this->request->param('act');
		// $GLOBALS['opt'] = $this->request->param('opt');
		// $GLOBALS['do'] = $this->request->param('do');
		// $event = controller($GLOBALS['mod'], "controller");
		// return $event->index($this->request);
    }
}
